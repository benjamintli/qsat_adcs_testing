filename = 'data.csv';

rawData = csvread (filename);
MagX = rawData (:,1);
MagY = rawData (:,2);
MagZ = rawData (:,3);

sz = 

scatter (MagX, MagY, 'r');
scatter (MagY, MagZ, 'g');
scatter (MagX, MagZ, 'b');
